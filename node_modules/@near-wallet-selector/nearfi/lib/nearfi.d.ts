import type { WalletModuleFactory, InjectedWallet } from "@near-wallet-selector/core";
import type { InjectedNearFi } from "./injected-nearfi";
declare global {
    interface Window {
        nearFiWallet: InjectedNearFi | undefined;
    }
}
export interface NearFiParams {
    iconUrl?: string;
    deprecated?: boolean;
}
export declare function setupNearFi({ iconUrl, deprecated, }?: NearFiParams): WalletModuleFactory<InjectedWallet>;
