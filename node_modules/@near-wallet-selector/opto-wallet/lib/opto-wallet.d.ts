import type { WalletModuleFactory, BrowserWallet } from "@near-wallet-selector/core";
declare global {
    interface Window {
        opto: object | undefined;
    }
}
export interface OptoWalletParams {
    walletUrl?: string;
    iconUrl?: string;
    deprecated?: boolean;
}
export declare function setupOptoWallet({ walletUrl, iconUrl, deprecated, }?: OptoWalletParams): WalletModuleFactory<BrowserWallet>;
