import type { WalletModuleFactory, InjectedWallet } from "@near-wallet-selector/core";
import type { InjectedCoin98 } from "./injected-coin98-wallet";
declare global {
    interface Window {
        coin98: InjectedCoin98;
    }
}
export interface Coin98WalletParams {
    iconUrl?: string;
    deprecated?: boolean;
}
export declare const setupCoin98Wallet: ({ iconUrl, deprecated, }?: Coin98WalletParams) => WalletModuleFactory<InjectedWallet>;
