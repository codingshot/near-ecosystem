import type { WalletModuleFactory, InjectedWallet } from "@near-wallet-selector/core";
import type { WalletProvider, WelldoneWalletParams } from "./injected-welldone";
export declare const STORAGE_ACCOUNT = "account";
declare global {
    interface Window {
        dapp: WalletProvider | undefined;
    }
}
export declare function setupWelldoneWallet({ iconUrl, deprecated, }?: WelldoneWalletParams): WalletModuleFactory<InjectedWallet>;
