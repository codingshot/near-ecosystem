import type { Network, Optional, Transaction } from "@near-wallet-selector/core";
import BN from "bn.js";
import { keyStores, WalletConnection, transactions as nearTransactions } from "near-api-js";
interface HereWalletState {
    wallet: WalletConnection;
    keyStore: keyStores.BrowserLocalStorageKeyStore;
}
export interface HereConfiguration {
    hereWallet: string;
    hereContract: string;
}
export declare const hereConfigurations: Record<string, HereConfiguration>;
declare const setupWalletState: (config: HereConfiguration, network: Network) => Promise<HereWalletState>;
declare const getHereBalance: (state: HereWalletState, config: HereConfiguration) => Promise<BN>;
declare const transformTransactions: (state: HereWalletState, transactions: Array<Optional<Transaction, "signerId">>) => Promise<nearTransactions.Transaction[]>;
export { setupWalletState, getHereBalance, transformTransactions };
